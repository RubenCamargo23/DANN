apiVersion: apps/v1
kind: Deployment
metadata:
  name: posts-db
  namespace: default
  labels:
    app: posts-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: posts-db
  template:
    metadata:
      labels:
        app: posts-db
    spec:
      containers:
        - name: posts-db
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
            - name: POSTGRES_DB
              value: posts_db
          volumeMounts:
            - name: posts-db-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: posts-db-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: posts-db
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: posts-db
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: posts-db-netpol
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: posts-db
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: posts-app
      ports:
        - protocol: TCP
          port: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: posts-app
  namespace: default
  labels:
    app: posts-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: posts-app
  template:
    metadata:
      labels:
        app: posts-app
    spec:
      containers:
        - name: posts-app
          image: posts_app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: DB_HOST
              value: posts-db
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: postgres
            - name: DB_PASSWORD
              value: postgres
            - name: DB_NAME
              value: posts_db
---
apiVersion: v1
kind: Service
metadata:
  name: posts-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: posts-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30003
